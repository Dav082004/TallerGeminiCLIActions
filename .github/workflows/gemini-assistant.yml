name: 💬 Gemini CLI Assistant

# =============================================================
# WORKFLOW: Gemini CLI - Asistente General
# =============================================================
# Este workflow proporciona un asistente general que responde preguntas
# y ofrece ayuda sobre el código, arquitectura, mejores prácticas, etc.
#
# ¿Qué hace?
# - Responde a menciones @gemini-cli SIN comandos específicos
# - Proporciona asesoramiento general sobre código y proyectos
# - Excluye comandos específicos (/triage, /review) que tienen workflows dedicados
# =============================================================

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  pull_request_review:
    types: [submitted]
  workflow_dispatch:
    inputs:
      comment_body:
        description: "Pregunta para el asistente"
        required: true
        type: string
      issue_number:
        description: "Número del issue o PR"
        required: false
        type: string

jobs:
  assistant:
    name: Asistente Gemini
    runs-on: ubuntu-latest
    timeout-minutes: 15

  # ----------------------------------------------------------------------
  # CONDICIÓN DE EJECUCIÓN (if):
  # Solo ejecutar cuando se mencione @gemini-cli SIN comandos específicos (/triage, /review)
  # Nota: se excluyen los comandos /triage y /review porque existen workflows separados
  # que manejan esas acciones con prompts más especializados y permisos distintos.
  # 
  # Casos de activación:
  # 1. Ejecución manual (workflow_dispatch)
  # 2. @gemini-cli en comentarios de issues (sin /triage ni /review)
  # 3. @gemini-cli en comentarios de PRs (sin /triage ni /review)  
  # 4. @gemini-cli en reviews de PRs (sin /triage ni /review)
  # ----------------------------------------------------------------------
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'issue_comment' && 
       contains(github.event.comment.body, '@gemini-cli') && 
       !contains(github.event.comment.body, '/triage') &&
       !contains(github.event.comment.body, '/review')) ||
      (github.event_name == 'pull_request_review_comment' && 
       contains(github.event.comment.body, '@gemini-cli') && 
       !contains(github.event.comment.body, '/triage') &&
       !contains(github.event.comment.body, '/review')) ||
      (github.event_name == 'pull_request_review' && 
       contains(github.event.review.body, '@gemini-cli') && 
       !contains(github.event.review.body, '/triage') &&
       !contains(github.event.review.body, '/review'))

    permissions:
      contents: write           # ✅ CAMBIADO: Ahora puede escribir archivos
      issues: write
      pull-requests: write
      actions: read             # ✅ AGREGADO: Para leer workflow info

  # ----------------------------------------------------------------------
  # PERMISOS ACTUALIZADOS:
  # - contents:write       → ✅ NUEVO: Permite escribir archivos, hacer commits y push
  # - issues:write         → ✅ Comentar en issues
  # - pull-requests:write  → ✅ Comentar en PRs
  # - actions:read         → ✅ NUEVO: Leer información de workflows
  # ----------------------------------------------------------------------
  # - contents:read -> permite leer archivos del repo si Gemini solicita contexto.
  # - issues:write -> necesario para publicar respuestas en issues/PRs.
  # - pull-requests:write -> necesario si el asistente crea comentarios en PRs.

    steps:
      - name: Checkout repository
        # Paso: traer el código del repositorio al runner. fetch-depth: 0 para poder
        # acceder al historial completo si el asistente lo solicita mediante coreTools.
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Obtener contexto del comentario
        # Paso: construir un JSON con los datos relevantes (comentario, título, cuerpo,
        # autor y nombre del repo). Este contexto se pasa al prompt para respuestas
        # más ricas y específicas.
        id: get-context
        uses: actions/github-script@v7
        with:
          script: |
            const context_data = {
              comment_body: context.payload.comment?.body || 
                           context.payload.review?.body || 
                           context.payload.inputs?.comment_body || '',
              issue_title: context.payload.issue?.title || '',
              issue_body: context.payload.issue?.body || '',
              pr_title: context.payload.pull_request?.title || '',
              pr_body: context.payload.pull_request?.body || '',
              author: context.payload.comment?.user?.login || 
                      context.payload.review?.user?.login ||
                      context.actor,
              repository_name: context.repo.repo,
              repository_owner: context.repo.owner
            };
            
            return JSON.stringify(context_data);

      - name: Ejecutar Gemini CLI Asistente
        # Paso: invocar la acción `run-gemini-cli` con el prompt adaptado al repo.
        # ✅ ACTUALIZADO: Ahora puede modificar archivos y hacer commits automáticamente
        # - `settings.coreTools` incluye herramientas de escritura de archivos
        # - `maxSessionTurns` limita la longitud de la sesión para evitar loops
        id: gemini-assistant
        uses: google-github-actions/run-gemini-cli@v0.1.10  # ✅ VERSIÓN CORRECTA
        with:
          prompt: |
            Eres un asistente de desarrollo de software inteligente y útil especializado en este proyecto TallerGeminiCLIActions.

            **CONTEXTO DEL PROYECTO:**
            ${{ steps.get-context.outputs.result }}

            **TU MISIÓN:**
            Ayudar a desarrolladores con preguntas técnicas, explicaciones de código, y sugerencias de mejora relacionadas con GitHub Actions, Gemini CLI, y desarrollo web frontend.

            **CAPACIDADES PRINCIPALES:**
            💡 **Análisis de código**: Explicar funciones, patrones y arquitectura
            � **Solución de problemas**: Debugging, errores, mejores prácticas
            📚 **Educación técnica**: Conceptos, algoritmos, tecnologías web
            🎯 **Recomendaciones**: Mejoras de código, herramientas, optimizaciones
            📝 **Documentación**: Ayuda con README, comentarios, guías de usuario
            🤖 **GitHub Actions**: Workflows, automatización, integración CI/CD

            **COMANDOS ESPECIALIZADOS:**
            - `/explicar [concepto]` - Explica tecnologías, patrones o código
            - `/sugerir [contexto]` - Propone mejoras o alternativas
            - `/documentar [elemento]` - Ayuda con documentación
            - `/analizar [problema]` - Analiza issues o errores específicos
            - `/optimizar [código]` - Sugiere optimizaciones de rendimiento

            **GUIDELINES:**
            ✅ Responde SIEMPRE en español
            ✅ Sé específico y proporciona ejemplos de código
            ✅ Considera el contexto educativo del proyecto (es un taller)
            ✅ Sugiere recursos adicionales para aprender más
            ✅ Usa emojis para claridad visual
            ✅ Si necesitas más contexto del repositorio, puedes solicitar archivos específicos

            **FORMATO DE RESPUESTA:**
            
            🤖 **Asistente Gemini CLI**
            
            [Tu análisis y respuesta detallada aquí]
            
            💡 **Recursos adicionales:**
            [Enlaces o sugerencias de aprendizaje cuando sea relevante]
            
            ---
            🎯 **Tip**: Usa comandos como `/explicar`, `/sugerir`, `/analizar` para respuestas más precisas.

            Analiza la consulta del usuario y proporciona una respuesta útil, educativa y contextual.

          gemini_api_key: ${{ secrets.GEMINI_API_KEY }}
          settings: |-
            {
              "coreTools": [
                "read_file",
                "write_file",
                "create_file", 
                "edit_file",
                "run_shell_command(ls)",
                "run_shell_command(find)",
                "run_shell_command(grep)",
                "run_shell_command(cat)",
                "run_shell_command(head)",
                "run_shell_command(tail)",
                "run_shell_command(wc)",
                "run_shell_command(tree)",
                "run_shell_command(echo)",
                "run_shell_command(git add)",
                "run_shell_command(git commit)",
                "run_shell_command(git push)",
                "run_shell_command(gh)"
              ],
              "maxSessionTurns": 15
            }

      - name: Responder al comentario
        # Paso: si el asistente respondió con éxito, publicar esa respuesta en el issue/PR.
        # Se determina el número del issue/PR para comentar; si no existe, se hace solo log.
        if: success()
        uses: actions/github-script@v7
        env:
          GEMINI_RESPONSE: ${{ steps.gemini-assistant.outputs.summary }}
        with:
          script: |
            const issueNumber = context.payload.issue?.number || 
                              context.payload.pull_request?.number ||
                              context.payload.inputs?.issue_number;
                              
            if (issueNumber) {
              const geminiResponse = process.env.GEMINI_RESPONSE || 'No se pudo obtener respuesta del asistente.';
              
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber,
                body: geminiResponse
              });
              
              console.log(`✅ Respuesta del asistente comentada en #${issueNumber}`);
              console.log(`📄 Respuesta: ${geminiResponse.substring(0, 200)}...`);
            } else {
              console.log('❌ No se pudo determinar el número de issue o PR');
            }

      - name: Comentar si hay error
        # Paso: en caso de fallo durante la ejecución, se publica un comentario amable
        # explicando el problema y enlazando a los logs para facilitar la investigación.
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            const issueNumber = context.payload.issue?.number || 
                              context.payload.pull_request?.number ||
                              context.payload.inputs?.issue_number;
                              
            if (issueNumber) {
              const errorMessage = `❌ **Error en el Asistente Gemini CLI**
              
              Hubo un problema al procesar tu solicitud. Por favor:
              
              1. 🔍 Verifica que la pregunta esté bien formateada
              2. 🔄 Intenta nuevamente en unos minutos
              3. 📋 [Ver logs detallados](${context.payload.repository.html_url}/actions/runs/${context.runId})
              
              💡 **Tip**: Para mejor ayuda, menciona \`@gemini-cli\` seguido de tu pregunta específica.`;
              
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber,
                body: errorMessage
              });
              
              console.log(`❌ Error reportado en #${issueNumber}`);
            }
